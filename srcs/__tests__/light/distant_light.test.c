#include "rt.test.h"

/*
static t_ll					ll;
static t_distant_light		dl;

TEST_GROUP(distant_light);

TEST_SETUP(distant_light)
{
	ll.light = ft_memalloc(sizeof(t_sphere));
	set_light();
	ll.get_dir = &distant_light_dir;
	ll.get_distance = &distant_light_distance();
	dl = (t_distant_light *)ll.light();
}

TEST_TEAR_DOWN(distant_light)
{
	ft_memdel((void **)&distant_light);
}

TEST()
{

}
*/
